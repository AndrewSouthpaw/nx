import { beforeAll } from 'vitest';
import { setProjectAnnotations } from '<%= uiFramework %>';
import * as projectAnnotations from './preview';

const project = setProjectAnnotations([projectAnnotations]);

beforeAll(project.beforeAll);
